name: seam_scroll
description: "sheet 跟內層 scrollable 的物理協同 primitive。"
version: 0.0.1

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
