name: seam_scroll
description: "Flutter scroll 協同 primitive：snap anchor + velocity 接力，原生感的 DraggableScrollableSheet / NestedScrollView 替代品。"
version: 0.2.0
homepage: https://github.com/heyclf/seam_scroll
repository: https://github.com/heyclf/seam_scroll
issue_tracker: https://github.com/heyclf/seam_scroll/issues

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
